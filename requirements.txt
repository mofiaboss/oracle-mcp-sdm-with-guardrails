# Oracle MCP Server Dependencies

# MCP Protocol
mcp>=1.0.0

# FastMCP for rapid development (optional)
fastmcp>=0.1.0

# Note: Java and JDBC driver are not Python dependencies
# Java 21+ required (install separately)
# ojdbc11-23.5.0.24.07.jar required (download from Oracle)
# json-20240303.jar required (download from Maven Central)
