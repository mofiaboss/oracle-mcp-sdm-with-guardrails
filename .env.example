# Oracle Database Connection Configuration
# Copy this file to .env and fill in your actual values

# Database Host (localhost if using StrongDM proxy)
ORACLE_HOST=127.0.0.1

# Database Port (10006 is common for StrongDM, 1521 is Oracle default)
ORACLE_PORT=10006

# Oracle Service Name (e.g., ORCL, XE, or your custom service name)
ORACLE_SERVICE_NAME=ylvoprd

# Database Credentials
ORACLE_USER=your_username
ORACLE_PASSWORD=your_password

# Optional: Java Home (if not in PATH)
# JAVA_HOME=/opt/homebrew/opt/openjdk@21

# Optional: JDBC Driver Path (if not in project directory)
# JDBC_JAR_PATH=/path/to/ojdbc11-23.5.0.24.07.jar
